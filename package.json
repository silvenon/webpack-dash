{
  "private": true,
  "scripts": {
    "update": "cd webpack.js.org && git pull && cd -",
    "build:frontend": "cd webpack.js.org && yarn && yarn run fetch && yarn run build && cd -",
    "build:docset": "eslint . --fix && node index.js",
    "build": "yarn run build:frontend && yarn run build:docset"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "cheerio": "^0.22.0",
    "eslint": "^3.10.2",
    "eslint-config-standard": "^7.0.1",
    "eslint-plugin-promise": "^3.4.0",
    "eslint-plugin-standard": "^2.0.1",
    "fs-promise": "^2.0.0",
    "lodash": "^4.17.4",
    "sequelize": "^3.26.0",
    "shelljs": "^0.7.5",
    "sqlite3": "^3.1.8"
  },
  "eslintConfig": {
    "extends": "standard",
    "rules": {
      "comma-dangle": [
        "error",
        "always-multiline"
      ]
    }
  }
}
